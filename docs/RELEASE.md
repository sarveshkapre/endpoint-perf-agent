# RELEASE

## Process
1. Run `make check` and ensure CI is green.
2. Update `docs/CHANGELOG.md` with shipped changes.
3. Tag a release: `git tag -a vX.Y.Z -m "vX.Y.Z"`.
4. Push tag and create GitHub Release notes.
